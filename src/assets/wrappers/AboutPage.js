import styled from 'styled-components';

const Wrapper = styled.div`
  h2 {
    margin-top:1rem;
    margin-left:1rem;
  }
  p {
    line-height: 2;
    color: var(--grey-500);
    margin-top: 1rem;
    margin-left: 1rem;
  }
`;

export default Wrapper;
